# Telegram credentials — configure only in the hosting provider (Vercel/Netlify)
# No BACKEND_URL is needed: the frontend calls same-origin /api/... routes.
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_PHONE=
TELEGRAM_SESSION=
ADMIN_TELEGRAM_USER_ID=

# Existing PostgreSQL connection. Required: the one-time Telegram login is
# stored here, so without it the owner would have to log in again every time.
DATABASE_URL=

# Generate long random values; never expose these to the frontend.
# Rotating AUTH_JWT_SECRET also revokes every saved owner console session.
SESSION_ENCRYPTION_KEY=
AUTH_JWT_SECRET=

# Optional. The Telegram console now uses a simple OTP login and no longer
# needs a setup secret; keep this set only if you also use the trusted CLI.
ADMIN_SETUP_SECRET=

# How many days a device stays logged in to the owner Telegram console (default 180)
OWNER_SESSION_DAYS=180

# Optional private Telegram source identifier, server-side only
TELEGRAM_SOURCE_CHANNEL=

# ─── Discord media import (post on Discord → shows in Premium) ───
# This is the ONLY variable needed for media import:
#   1. Discord Developer Portal → your app → Bot → Reset Token → copy it here.
#   2. On the portal's "Invite Your Bot" page enable View Channel + Read
#      Message History, choose the x-sutra server, Authorize.
#   3. Set DISCORD_BOT_TOKEN below and redeploy.
# DISCORD_GUILD_ID defaults to the x-sutra server (1542540297005834242);
# only set it if you import from a different server.
DISCORD_BOT_TOKEN=
# DISCORD_GUILD_ID=1542540297005834242
# Optional: mirror imported files into the premium file store instead of
# streaming them from the Discord CDN (off by default).
# DISCORD_STORE_ATTACHMENTS=false

# ─── Discord web login (real OAuth2 login for Premium) ───
# Users sign in with their normal Discord account — no bot is involved.
# Setup (one time):
#   1. Create an application: https://discord.com/developers/applications
#   2. OAuth2 → copy the Client ID and Client Secret below.
#   3. OAuth2 → Redirects: add every domain the app can be served from:
#        https://<your-netlify-domain>
#        https://<your-vercel-domain>
#        http://localhost:5173        (local development only)
# The client secret is used by the server functions only; it never reaches
# the browser. Without these two values the login card shows a friendly
# "not configured" notice instead of a broken redirect.
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
